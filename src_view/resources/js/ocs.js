import _ from 'lodash';
import http from '../utils/http';
import { resultCode } from '../utils/constant';
import $ from 'jquery';



/**
 * 
 * @param {string} nowData now or before
 * @description
 * 이전 OCS, 현재 OCS 데이터 불러오기 
 */
function getOcsData(nowData = "now") {

    http.getMethod('/')
}
